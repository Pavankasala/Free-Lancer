version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - ./backend/lemons.db:/app/lemons.db
    environment:
      - PORT=5000
    restart: always
